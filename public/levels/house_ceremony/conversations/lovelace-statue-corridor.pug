-
  const STATE_KEY = 'TQ_HOUSE_CEREMONY_WORLD_STATE';
  const worldState = getState(STATE_KEY);

conversation(display_name="에이다 러브레이스 석상")
  case lastResponse

    default
        statement#default

  statements
    statement#default
      text.
        홀에 소문이 퍼지고 있어요… 기숙사 대항전의 비밀 방… 사라진 학생… 위험에 처한 API 비전술 아카데미…

// Flush world state changes, if any
- setState(STATE_KEY, worldState);
