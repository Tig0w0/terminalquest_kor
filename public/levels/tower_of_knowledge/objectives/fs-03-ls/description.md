<% const isWindows = context.systemInfo.os === 'win32'; %>

# 다른 디렉터리 또는 파일 찾기

<div class="aside">
<h3>요구 사항</h3>
<ul>
  <li>터미널을 여세요.</li>
  <li><b>ls</b> 명령어를 입력하세요.</li>
  <li>표시된 파일이나 디렉터리 중 하나의 이름을 오른쪽 입력란에 복사하세요.</li>
  <li>완료하면 <em>해킹</em>을 누르세요.</li>
</ul>
</div>

터미널의 홈 디렉터리가 파일 시스템의 어디에 있는지 알았으니, 그곳에 무엇이 있는지 확인해 봅시다.

<% if(isWindows) { %>

두 번째 명령어 `ls`를 배워 봅시다. 이 명령어는 디렉터리의 내용물을 **나열(List)**합니다.

실행하면 현재 작업 디렉터리에 있는 파일과 디렉터리 이름이 표시됩니다. 터미널에 `ls`를 입력하고 나타난 이름 중 하나를 오른쪽 입력란에 복사한 뒤 _해킹_ 버튼을 누르세요.

<% } else { %>

두 번째 명령어 `ls`를 배워 봅시다. 이 명령어는 디렉터리의 내용물을 **나열(List)**합니다.

실행하면 현재 작업 디렉터리에 있는 파일과 디렉터리 이름이 표시됩니다. 터미널에 `ls`를 입력하고 나타난 이름 중 하나를 오른쪽 입력란에 복사한 뒤 _해킹_ 버튼을 누르세요.

<% } %>
